@using Microsoft.AspNetCore.Mvc.Localization
@using ZeldaArena.Application.Common.Models
@using ZeldaArena.Application.Features.Admin.Matches.Queries.GetMatchesForAdmin
@using ZeldaArena.Domain.Enums
@model ZeldaArena.Web.Areas.Admin.Models.Matches.MatchIndexViewModel
@inject IHtmlLocalizer<SharedResource> T
@{
    ViewData["Title"] = T["admin.nav.matches"].Value;
    var filter = Model.Filter;
}

<div class="admin-toolbar">
    <h1 class="admin-heading">@T["admin.nav.matches"]</h1>
    <a class="btn-solid btn-solid--sm" asp-action="Create">@T["admin.match.create"]</a>
</div>

<form class="filters" method="get" asp-action="Index" role="search" data-filter-form aria-label="@T["filter.title"]">
    <input type="hidden" name="sort" value="@Model.Sort" data-list-state data-default="@AdminMatchSorting.Map.DefaultKey" />

    <div class="row g-3">
        <div class="col-12 col-md-5">
            <label class="form-label" for="tournamentId">@T["admin.match.tournament"]</label>
            <select class="form-select" id="tournamentId" name="tournamentId">
                <option value="">@T["filter.any"]</option>
                @foreach (var tournament in Model.Tournaments)
                {
                    <option value="@tournament.Id" selected="@(filter.TournamentId == tournament.Id)">@tournament.Name</option>
                }
            </select>
        </div>
        <div class="col-12 col-md-4">
            <label class="form-label" for="status">@T["admin.common.status"]</label>
            <select class="form-select" id="status" name="status">
                <option value="">@T["filter.any"]</option>
                @foreach (var status in Enum.GetValues<MatchStatus>())
                {
                    <option value="@status" selected="@(filter.Status == status)">@T[EnumResourceKeys.For(status)]</option>
                }
            </select>
        </div>
        <div class="col-12 col-md-3">
            <label class="form-label" for="pageSize">@T["filter.page_size"]</label>
            <select class="form-select" id="pageSize" name="pageSize" data-default="@PageSizes.Default" data-no-chip>
                @foreach (var size in PageSizes.Allowed)
                {
                    <option value="@size" selected="@(size == filter.NormalizedPageSize)">@size</option>
                }
            </select>
        </div>
    </div>

    <div class="chips" data-filter-chips tabindex="-1" role="group" hidden
         aria-label="@T["filter.active"]" data-remove-label="@T["filter.remove_chip"]"></div>

    <div class="filters__actions">
        <button class="btn-solid" type="submit">@T["filter.apply"]</button>
        <a class="filters__reset" asp-action="Index" data-filter-reset>@T["filter.reset"]</a>
    </div>
</form>

<div id="match-table" data-list data-api="@Url.Action("Index", "AdminMatchesApi", new { area = "Api" })">
    <partial name="_MatchTable" model="Model" />
</div>
