@page
@model AccessDeniedModel
@{
    ViewData["Title"] = T["access_denied.title"].Value;
}

<div class="alert alert-danger" role="alert">@T["access_denied.message"]</div>

<a asp-page="/Index" asp-area="" class="btn btn-outline-primary">ZeldaArena</a>
