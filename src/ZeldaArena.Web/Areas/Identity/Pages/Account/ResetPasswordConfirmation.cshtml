@page
@model ResetPasswordConfirmationModel
@{
    ViewData["Title"] = T["reset_password.title"].Value;
}

<div class="alert alert-success" role="alert">@T["reset_password.done"]</div>

<a asp-page="./Login" class="btn btn-primary">@T["login.submit"]</a>
