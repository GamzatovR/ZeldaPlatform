@page
@model LockoutModel
@{
    ViewData["Title"] = T["lockout.title"].Value;
}

<div class="alert-token alert-token--warning" role="alert">@T["lockout.message"]</div>

<a asp-page="./ForgotPassword" class="btn-ghost">@T["login.forgot_password"]</a>
