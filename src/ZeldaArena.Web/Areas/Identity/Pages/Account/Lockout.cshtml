@page
@model LockoutModel
@{
    ViewData["Title"] = T["lockout.title"].Value;
}

<div class="alert alert-warning" role="alert">@T["lockout.message"]</div>

<a asp-page="./Login" class="btn btn-outline-primary">@T["login.title"]</a>
