@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = T["register_confirmation.title"].Value;
}

<p>@T["register_confirmation.message"]</p>

<p class="text-muted">@Model.Email</p>

<a asp-page="./Login" class="btn btn-outline-primary">@T["login.submit"]</a>
