<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>ZeldaArena.Domain</RootNamespace>
    <!-- Ядро собирается без предупреждений. -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <!-- Зависимостей нет и не будет: Domain использует только BCL, это ловит DependencyRuleTests. -->

</Project>
