namespace ZeldaArena.Domain.Enums;

/// <summary>К чему оставлен комментарий. Полиморфная связь без FK — см. docs/erd.md.</summary>
public enum CommentTargetType
{
    NewsArticle = 0,
    Match = 1,
}