# Журнал работ ZeldaArena

Обновляется в конце каждой фазы. План фаз — `docs/SPEC.md` §18.

## Состояние фаз

| Фаза | Содержание | Ветка | Статус |
|---|---|---|---|
| 0 | Фундамент: решение, проекты, CPM, архитектурные тесты, docker-compose, CI | `feature/phase-0-foundation` | **Готово** |
| 1 | Домен и БД: сущности, VO, события, `AppDbContext`, миграция, сид, `erd.md` | `feature/phase-1-domain` | **Готово** |
| 2 | Каркас Application: порты, `Result`, `PagedResult`, MediatR + behaviors, репозитории | `feature/phase-2-application` | Следующая |
| 3 | Identity и безопасность: роли, 2FA, письма, lockout, rate limiting | `feature/phase-3-identity` | — |
| 4 | Подписки, фича-гейт, мнимая оплата | `feature/phase-4-billing` | — |
| 5 | Дизайн-система и каркас UI | `feature/phase-5-design` | — |
| 6 | Киберспортивный модуль | `feature/phase-6-esports` | — |
| 7 | Магазин | `feature/phase-7-shop` | — |
| 8 | AJAX, фильтры, пагинация | `feature/phase-8-ajax` | — |
| 9 | Админ-панель | `feature/phase-9-admin` | — |
| 10 | SignalR, MongoDB, логирование | `feature/phase-10-realtime` | — |
| 11 | Локализация, ошибки, middleware, деплой, документация | `feature/phase-11-final` | — |

---

## Фаза 0 — Фундамент

### Сделано

**Решение и проекты.** `ZeldaArena.sln`, четыре проекта в `src/` и два в `tests/`, ссылки
расставлены по правилу зависимостей `docs/SPEC.md` §5.2. `Web` создан по шаблону `mvc`:
приложение запускается с первого дня. Шаблонные `wwwroot/lib` (bootstrap, jquery) остаются
временно и будут заменены ресурсами макета в Фазе 5.

**Конфигурация сборки.** `global.json` фиксирует SDK 10.0.400 с `rollForward: latestFeature`.
`Directory.Build.props` задаёт `net10.0`, C# 14, nullable, `EnforceCodeStyleInBuild` и
`InvariantGlobalization=false` (иначе не заработает локализация ru/en из §9.5).
`TreatWarningsAsErrors` включён только в `Domain` и `Application`, как требует `CLAUDE.md`.
`Directory.Packages.props` вводит Central Package Management и содержит версии **всего** стека
из §2, а не только используемого сейчас: дальше фазы добавляют `PackageReference` без версии.

**Архитектурные тесты — 8 тестов, все пять правил §5.2:**

| Тест | Правило |
|---|---|
| `Domain_should_not_depend_on_other_layers` | 1, по типам |
| `Domain_assembly_should_not_reference_forbidden_packages` | 1, по ссылкам сборки |
| `Application_should_not_depend_on_infrastructure_web_or_frameworks` | 2, по типам |
| `Application_assembly_should_not_reference_forbidden_packages` | 2, по ссылкам сборки |
| `Infrastructure_should_not_depend_on_web` | дополнительно |
| `Web_should_not_use_infrastructure_types_outside_composition_root` | 3 |
| `Controllers_should_only_inject_sender_localizer_and_logger` | 4 |
| `Domain_types_should_not_expose_public_setters` | 5 |

**Проверка тестов на «не вакуумность».** Правила проверены внесением реальных нарушений
с последующим откатом (в коммиты не попало):

| Проба | Результат |
|---|---|
| `PackageReference` на EF Core в `Domain`, тип не используется | проверка по типам молчит, **проверка по ссылкам сборки краснеет** — ради этого она и добавлена |
| Тип `Domain`, использующий `DbContext`, со свойством `{ get; set; }` | 3 красных теста: правило 1 по типам, правило 1 по ссылкам, правило 5 |
| Контроллер с `HttpClient` в конструкторе | красное правило 4 |
| Контроллер, использующий тип `Infrastructure` | красное правило 3 |

**Инфраструктура разработки.** `deploy/docker-compose.yml`: postgres 17, mongo 8, mailhog,
у первых двух healthcheck и именованные тома. Параметры вынесены в переменные, образец —
`deploy/.env.example`; сам `.env` в `.gitignore`.

**CI.** `.github/workflows/ci.yml`: restore → build → test → `dotnet format --verify-no-changes`
на push в `main` и на каждый PR.

**Документация.** `README.md` (быстрый старт, структура, порты), `docs/adr/ADR-0001`
(Clean Architecture и проверка правила тестами), `docs/adr/ADR-0002` (Shouldly, MediatR 12.x,
Mapster — выбор по лицензии), индекс ADR.

### Проверено

| Проверка | Результат |
|---|---|
| `dotnet build -c Release` | успешно, 0 предупреждений, 0 ошибок |
| `dotnet test` | 9 тестов пройдено (8 архитектурных + smoke), 0 упало |
| `dotnet format --verify-no-changes` | чисто |
| Негативные пробы на все пять правил | каждая даёт красный тест, см. таблицу выше |
| `docker compose up -d` | три контейнера подняты, postgres и mongo — `healthy` |
| MailHog | `http://localhost:8025` отвечает 200 |
| PostgreSQL | подключение с хоста строкой из `appsettings.Development.json` (порт 5433) успешно, PostgreSQL 17.11 |
| MongoDB | `db.adminCommand('ping').ok` → 1 |
| `dotnet run --project src/ZeldaArena.Web` | приложение отдаёт 200 на `/`, `AddInfrastructure()` вызывается без ошибок |

### Принятые решения

**MediatR 12.5.0, а не 14.x** — последняя версия под Apache-2.0; с 13-й лицензия коммерческая.
Обоснование в ADR-0002.

**PostgreSQL наружу портом 5433.** Порт 5432 на машине разработки занят локально
установленным сервером. Внутри compose-сети всё по-прежнему `postgres:5432`, наружу — 5433;
строка подключения в `appsettings.Development.json` это учитывает.

**`GenerateDocumentationFile=true` для всех проектов.** Иначе анализатор IDE0005 («лишний
using») не работает при сборке. `CS1591` (нет XML-комментария у публичного члена) подавлен —
комментировать требуется по смыслу, а не поголовно.

**`.sln`, а не `.slnx`.** Шаблон .NET 10 по умолчанию создаёт XML-формат `slnx`; выбран
классический `.sln` ради совместимости с привычным инструментарием.

### Входные условия Фазы 1

- Сущности, value objects, enum'ы и доменные события из `docs/SPEC.md` §5.3 и §6.
- `AppDbContext`, конфигурации через `IEntityTypeConfiguration<T>`, первая миграция,
  идемпотентный сид по §6.
- `docs/erd.md`, unit-тесты инвариантов домена.
- **Sentinel-тест.** По §18 он утверждает, что сборки `Domain` и `Application` содержат типы.
  Формулировать его надо аккуратно: в обеих сборках уже лежат якоря
  `DomainAssemblyReference` и `ApplicationAssemblyReference`, поэтому проверка «есть хоть
  один тип» пройдёт вхолостую. Тест должен требовать наличие наследников `BaseEntity`
  в `Domain` и реализаций `IRequest<>` / хендлеров в `Application`.

### Открытые вопросы

Незакрытых вопросов по Фазе 0 нет.

Замечание на будущее: загрузка образов с Docker Hub на этой машине идёт очень медленно
(первый `docker compose pull` занял около двадцати минут). Образы теперь в локальном кэше,
повторный запуск мгновенный, но при работе на чистой машине это стоит учитывать.

---

## Фаза 1 — Домен и БД

### Сделано

**Домен.** 21 сущность, 4 объекта-значения (`Money`, `Slug`, `CountryCode`,
`ShippingAddress`), 13 enum'ов, 5 доменных событий, константы `FeatureCodes` и `PlanCodes`.
Публичных сеттеров нет ни у одной сущности: состояние меняют только методы, защищающие
инварианты. Нарушение инварианта — `InvariantViolationException` с кодом ресурса, чтобы
сообщение переводилось на языке пользователя, а не приходило готовой строкой из кода.

Ключевые инварианты и где они проверяются:

| Правило | Где |
|---|---|
| Завершённый матч не редактируется, счёт не выходит за формат серии | `Match.UpdateScore` |
| Победитель выводится из счёта, а не приходит извне | `Match.Finish` |
| Команда не играет сама с собой, `BestOf` нечётный | `Match.Schedule` |
| Уход из команды закрывает запись состава, а не удаляет её | `Team.RemovePlayer` |
| Количество в корзине не больше остатка, цена берётся у товара | `Cart.AddItem` |
| Итог заказа пересчитывается внутри сущности, скидка не больше суммы | `Order.Recalculate` |
| Продление добавляет дни к оплаченному сроку, а не к «сегодня» | `Subscription.Extend` |
| Код подтверждения сверяется хешем за постоянное время, попытки ограничены | `Payment.Confirm` |

**База.** `AppDbContext` наследует `IdentityDbContext`, конфигурации только через
`IEntityTypeConfiguration`, миграция `InitialCreate` создаёт 31 таблицу. Схема описана
в `docs/erd.md`, решения — в `docs/adr/ADR-0003`.

**Сид.** Идемпотентный, наборы ищутся по естественным ключам. 3 тарифа, 2 фичи,
4 привязки, 3 категории, 24 товара, 12 команд, 60 игроков с составами, 4 турнира
с 24 участниками, 40 матчей во всех пяти статусах (из них 2 Live) и 200 записей
статистики игроков.

**Тесты.** 120 unit-тестов домена и sentinel-тест в архитектурных.

### Проверено

| Проверка | Результат |
|---|---|
| `dotnet build -c Release` | успешно, 0 предупреждений, 0 ошибок |
| `dotnet test` | 135 тестов пройдено (120 доменных + 15 архитектурных), 0 упало |
| `dotnet format --verify-no-changes` | чисто |
| `dotnet ef database update` на чистой БД | миграция применяется без ошибок |
| Число таблиц | 31: 7 `AspNet*` + 10 киберспорт + 6 магазин + 5 биллинг + 3 общих |
| Типы столбцов | деньги — `numeric(18,2)`, даты — `timestamp with time zone`, даты без времени — `date` |
| Правила удаления | `Matches → Teams/Tournaments`, `Orders → AspNetUsers` — `RESTRICT`, проверено по `pg_constraint` |
| Уникальные индексы | `Slug`, `Code`, `Sku`, `Number`, `IdempotencyKey`, `(UserId, TargetType, TargetId)`, частичные на `Carts` |
| Сид | первый запуск — данные из таблицы выше; второй запуск — ноль `INSERT`, счётчики не изменились |
| `dotnet run` | приложение поднимается, `/` отдаёт 200 |

### Принятые решения

Четыре неоднозначности `docs/SPEC.md` разобраны до написания кода, подробности
в `docs/adr/ADR-0003`:

1. **`ApplicationUser` заведён уже в Фазе 1.** Иначе восемь сущностей ссылались бы
   на пользователя без внешнего ключа, а Фаза 3 получила бы миграцию, дописывающую
   FK к таблицам с данными. `Domain` при этом чист: сущности хранят `Guid UserId`,
   связь объявлена только в конфигурациях `Infrastructure`.
2. **Добавлена таблица `Notifications`, итого 31 вместо 30.** §5.3 объявляет сущность
   `Notification`, §11 требует колокольчик со счётчиком непрочитанных, а в перечне
   таблиц §6 её нет. Счётчик обязан переживать перезагрузку страницы.
3. **Инвариант счёта:** каждый счёт ≤ `BestOf / 2 + 1`, сумма ≤ `BestOf`. Буквальное
   чтение «счёт не превышает `BestOf`» допускало бы в Bo3 счёт 3:0.
4. **Сид Фазы 1 — всё, кроме данных, требующих пользователя.** Новости не попали в сид
   вместе с пользователями: у новости обязателен автор с внешним ключом на `AspNetUsers`.
   Сидер расширяется в Фазе 3, а не переписывается.

**Sentinel-тест сформулирован только для `Domain`.** По `docs/SPEC.md` §18 он должен
утверждать и наличие `IRequest<>` в `Application`, но MediatR приходит в Фазе 2 —
сейчас такой тест был бы красным по замыслу, а таких в проекте не бывает. Тест требует
не меньше 20 наследников `BaseEntity`, наличие сущностей в каждой предметной области,
доменные события и объекты-значения. Половина про `Application` дописывается в Фазе 2.

**Запрет «игрок не может состоять в двух командах одновременно» проверяется только
внутри команды.** Это правило между агрегатами: сущность `Team` не видит чужие составы.
Кросс-командная проверка — валидатор сценария в Фазе 6, отметка стоит комментарием
в `Team.AddPlayer`.

**`dotnet-ef` закреплён в манифесте локальных инструментов** (`.config/dotnet-tools.json`,
версия 10.0.11 под EF Core 10.0.11). Перед первым запуском миграций нужен
`dotnet tool restore`; после этого команды из `CLAUDE.md` работают без изменений.

**Ошибка в сиде поймана инвариантом домена.** Первая версия сида добавляла участников
в уже завершённый турнир, и `Tournament.AddTeam` это отклонил. Порядок исправлен:
состав формируется, пока турнир анонсирован. Инварианты работают не только на входе
пользователя.

### Открытые вопросы

Незакрытых вопросов по Фазе 1 нет.

### Входные условия Фазы 2

- Порты в `Application/Common/Interfaces` по `docs/SPEC.md` §5.3: `IRepository<T>`,
  `IReadRepository<T>`, `INewsRepository`, `IUnitOfWork`, `ICurrentUserService`,
  `IDateTimeProvider` и остальные.
- `Result<T>` и `Error` **уже есть в `Domain/Common`** — в `Application/Common/Models`
  создаются только `PagedResult<T>` и `FilterBase`, дублировать не нужно.
- MediatR 12.5.0 и behaviors; тогда же — обёртка доменных событий в `INotification`
  и их диспетчеризация после `SaveChanges`.
- `AuditableEntityInterceptor` переключается с `TimeProvider` на порт `IDateTimeProvider`.
- Вторая половина sentinel-теста: наличие `IRequest<>` и хендлеров в `Application`.
- `EfRepository<T>` и `UnitOfWork` поверх готового `AppDbContext`.
